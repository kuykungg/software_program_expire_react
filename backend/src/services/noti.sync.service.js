import {softwareDB} from "../repositories/software.repo";
import {notiDB} from "../repositories/notify.repo";
export async function syncdata(){
    console.log("Sync srarted");
    const list = await
}
